def palindrome(s): # function with input "madam"
    res="" # empty string
    for i in s: 
        res = i + res
    if res == s:
        print(f"{i} is palindrome")
    else:
        print(f"{i} is not the plaindrome")            

if __name__ == "__main__":
    s="madam" # input
    palindrome(s)   
